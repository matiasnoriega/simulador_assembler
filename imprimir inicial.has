JMP iniciar;

inicial: DB "MATIAS";
DB 0;

iniciar:
MOV A, inicial; Apuntamos con A a la variable "inicial"
MOV B, 0xE8; Aputamos con el registro B a la primer posicion del output

imprimir: 
PUSH C;
PUSH D; 
MOV D, 0; Declaro una variable en 0 para comparar

loop:
MOV C, [A];
MOV [B], C;

HLT;